<%= form_for [ @post, @comment ] do |form| %>
  <div class="row">
    <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="col-md-4 col-sm-4 field">
      <%= form.label "New Comment" %>
      <%= form.text_field :content, class: "form-control", placeholder: "What's on your mind?" %>
    </div>

    <div class="col-md-4 col-sm-4 field">
      <%= form.label :author %>
      <%= form.text_field :author, class: "form-control", placeholder: "John Doe" %>
    </div>
  </div>
  <div class="actions" style="margin-top: 30px;">
    <%= form.submit "Comment", class: "btn btn-primary", id: "comment-submit" %>
  </div>
<% end %>

